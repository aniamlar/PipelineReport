pipeline {
   agent { label 'master' }
   stages {
       stage('before') {
           steps {
               println("before")
           }
       }
       stage('parallel run') {
           parallel {
             
              
              
              
           }
       }
       stage('after') {
           steps {
               println("after")
           }
       }
   }
}
